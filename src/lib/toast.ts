/* Small wrapper to expose the project's toast API in a single place.
   Use `import toast from '@/lib/toast'` or `import { toast } from '@/lib/toast'` in components.
*/
import { toast } from 'vue3-toastify'

export { toast }

export default toast
